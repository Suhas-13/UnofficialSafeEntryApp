<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto" android:layout_height="match_parent" android:layout_width="match_parent" android:id="@+id/threemenu">
    <item
        android:id="@+id/empty"
        android:title="options"
        android:orderInCategory="101"
        app:showAsAction="always"
        android:icon="@drawable/threedots">

        <menu>

            <item android:id="@+id/nric"
                app:showAsAction="ifRoom"
                android:title="NRIC" />
            <item android:id="@+id/phone"
                app:showAsAction="ifRoom"
                android:title="Phone Number" />
        </menu>

    </item>

</menu>